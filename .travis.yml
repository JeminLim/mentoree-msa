language: java
jdk:
  - openjdk11

branches:
  only:
    - master

cache:
  directories:
    - '$HOME/.m2/repository'
    - '$HOME/.gradle'

script:
  - "./gradlew :infra-api-gateway-service:build"

before_install:
  chmod +x gradlew

notifications:
  email:
    recipients:
      - wer0801@gmail.com